//! `MetadataResponse` — generated from `schemas/MetadataResponse.json`.
//!
//! @generated by `cargo xtask codegen`. Do not edit by hand.
//!
//! No tagged fields skipped.

#![allow(
    clippy::field_reassign_with_default,
    clippy::nonminimal_bool,
    clippy::derivable_impls
)]

use bytes::{Buf, BufMut};

#[allow(unused_imports)]
use crate::error::{DecodeError, EncodeError};
use crate::wire::{self, RawTaggedField};

/// Whether `version` of this message uses the flexible (compact) encoding.
pub fn is_flexible(version: i16) -> bool {
    let _ = version;
    version >= 9
}

/// `MetadataResponse`, versions 0-13.
#[derive(Debug, Clone, PartialEq)]
pub struct MetadataResponse {
    /// The duration in milliseconds for which the request was throttled due to a quota violation, or zero if the request did not violate any quota.
    pub throttle_time_ms: i32,
    /// A list of brokers present in the cluster.
    pub brokers: Vec<MetadataResponseBroker>,
    /// The cluster ID that responding broker belongs to.
    pub cluster_id: Option<String>,
    /// The ID of the controller broker.
    pub controller_id: i32,
    /// Each topic in the response.
    pub topics: Vec<MetadataResponseTopic>,
    /// 32-bit bitfield to represent authorized operations for this cluster.
    pub cluster_authorized_operations: i32,
    /// The top-level error code, or 0 if there was no error.
    pub error_code: i16,
    /// Tagged fields not (yet) known to this crate, kept raw.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for MetadataResponse {
    fn default() -> Self {
        Self {
            throttle_time_ms: 0,
            brokers: Vec::new(),
            cluster_id: None,
            controller_id: -1,
            topics: Vec::new(),
            cluster_authorized_operations: -2147483648,
            error_code: 0,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl MetadataResponse {
    pub const API_KEY: i16 = 3;
    pub const MIN_VERSION: i16 = 0;
    pub const MAX_VERSION: i16 = 13;

    pub fn encode(&self, buf: &mut impl BufMut, version: i16) -> Result<(), EncodeError> {
        if version >= 3 {
            buf.put_i32(self.throttle_time_ms);
        }
        if is_flexible(version) {
            wire::put_compact_array_len(buf, Some(self.brokers.len()));
        } else {
            wire::put_array_len(buf, Some(self.brokers.len()))?;
        }
        for item in &self.brokers {
            item.encode(buf, version)?;
        }
        if version >= 2 {
            if self.cluster_id.is_none() && !(version >= 2) {
                return Err(EncodeError::NullField("ClusterId"));
            }
            if is_flexible(version) {
                wire::put_compact_nullable_string(buf, self.cluster_id.as_deref());
            } else {
                wire::put_nullable_string(buf, self.cluster_id.as_deref())?;
            }
        }
        if version >= 1 {
            buf.put_i32(self.controller_id);
        }
        if is_flexible(version) {
            wire::put_compact_array_len(buf, Some(self.topics.len()));
        } else {
            wire::put_array_len(buf, Some(self.topics.len()))?;
        }
        for item in &self.topics {
            item.encode(buf, version)?;
        }
        if (8..=10).contains(&version) {
            buf.put_i32(self.cluster_authorized_operations);
        }
        if version >= 13 {
            buf.put_i16(self.error_code);
        }
        if is_flexible(version) {
            wire::put_tagged_fields(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }

    pub fn decode(buf: &mut impl Buf, version: i16) -> Result<Self, DecodeError> {
        let mut this = Self::default();
        if version >= 3 {
            this.throttle_time_ms = wire::get_i32(buf)?;
        }
        this.brokers = {
            let len = if is_flexible(version) {
                wire::get_compact_array_len(buf)?
            } else {
                wire::get_array_len(buf)?
            };
            match len {
                None => return Err(DecodeError::InvalidLength(-1)),
                Some(n) => {
                    let mut items = Vec::new();
                    for _ in 0..n {
                        items.push(MetadataResponseBroker::decode(buf, version)?);
                    }
                    items
                }
            }
        };
        if version >= 2 {
            this.cluster_id = {
                let raw = if is_flexible(version) {
                    wire::get_compact_nullable_string(buf)?
                } else {
                    wire::get_nullable_string(buf)?
                };
                if raw.is_none() && !(version >= 2) {
                    return Err(DecodeError::InvalidLength(-1));
                }
                raw
            };
        }
        if version >= 1 {
            this.controller_id = wire::get_i32(buf)?;
        }
        this.topics = {
            let len = if is_flexible(version) {
                wire::get_compact_array_len(buf)?
            } else {
                wire::get_array_len(buf)?
            };
            match len {
                None => return Err(DecodeError::InvalidLength(-1)),
                Some(n) => {
                    let mut items = Vec::new();
                    for _ in 0..n {
                        items.push(MetadataResponseTopic::decode(buf, version)?);
                    }
                    items
                }
            }
        };
        if (8..=10).contains(&version) {
            this.cluster_authorized_operations = wire::get_i32(buf)?;
        }
        if version >= 13 {
            this.error_code = wire::get_i16(buf)?;
        }
        if is_flexible(version) {
            this.unknown_tagged_fields = wire::get_tagged_fields(buf)?;
        }
        Ok(this)
    }
}

/// `MetadataResponseBroker` (nested in [`MetadataResponse`]).
#[derive(Debug, Clone, PartialEq)]
pub struct MetadataResponseBroker {
    /// The broker ID.
    pub node_id: i32,
    /// The broker hostname.
    pub host: String,
    /// The broker port.
    pub port: i32,
    /// The rack of the broker, or null if it has not been assigned to a rack.
    pub rack: Option<String>,
    /// Tagged fields not (yet) known to this crate, kept raw.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for MetadataResponseBroker {
    fn default() -> Self {
        Self {
            node_id: 0,
            host: String::new(),
            port: 0,
            rack: None,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl MetadataResponseBroker {
    pub fn encode(&self, buf: &mut impl BufMut, version: i16) -> Result<(), EncodeError> {
        buf.put_i32(self.node_id);
        if is_flexible(version) {
            wire::put_compact_string(buf, &self.host);
        } else {
            wire::put_string(buf, &self.host)?;
        }
        buf.put_i32(self.port);
        if version >= 1 {
            if self.rack.is_none() && !(version >= 1) {
                return Err(EncodeError::NullField("Rack"));
            }
            if is_flexible(version) {
                wire::put_compact_nullable_string(buf, self.rack.as_deref());
            } else {
                wire::put_nullable_string(buf, self.rack.as_deref())?;
            }
        }
        if is_flexible(version) {
            wire::put_tagged_fields(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }

    pub fn decode(buf: &mut impl Buf, version: i16) -> Result<Self, DecodeError> {
        let mut this = Self::default();
        this.node_id = wire::get_i32(buf)?;
        this.host = if is_flexible(version) {
            wire::get_compact_string(buf)?
        } else {
            wire::get_string(buf)?
        };
        this.port = wire::get_i32(buf)?;
        if version >= 1 {
            this.rack = {
                let raw = if is_flexible(version) {
                    wire::get_compact_nullable_string(buf)?
                } else {
                    wire::get_nullable_string(buf)?
                };
                if raw.is_none() && !(version >= 1) {
                    return Err(DecodeError::InvalidLength(-1));
                }
                raw
            };
        }
        if is_flexible(version) {
            this.unknown_tagged_fields = wire::get_tagged_fields(buf)?;
        }
        Ok(this)
    }
}

/// `MetadataResponseTopic` (nested in [`MetadataResponse`]).
#[derive(Debug, Clone, PartialEq)]
pub struct MetadataResponseTopic {
    /// The topic error, or 0 if there was no error.
    pub error_code: i16,
    /// The topic name. Null for non-existing topics queried by ID. This is never null when ErrorCode is zero. One of Name and TopicId is always populated.
    pub name: Option<String>,
    /// The topic id. Zero for non-existing topics queried by name. This is never zero when ErrorCode is zero. One of Name and TopicId is always populated.
    pub topic_id: [u8; 16],
    /// True if the topic is internal.
    pub is_internal: bool,
    /// Each partition in the topic.
    pub partitions: Vec<MetadataResponsePartition>,
    /// 32-bit bitfield to represent authorized operations for this topic.
    pub topic_authorized_operations: i32,
    /// Tagged fields not (yet) known to this crate, kept raw.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for MetadataResponseTopic {
    fn default() -> Self {
        Self {
            error_code: 0,
            name: Some(String::new()),
            topic_id: [0u8; 16],
            is_internal: false,
            partitions: Vec::new(),
            topic_authorized_operations: -2147483648,
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl MetadataResponseTopic {
    pub fn encode(&self, buf: &mut impl BufMut, version: i16) -> Result<(), EncodeError> {
        buf.put_i16(self.error_code);
        if self.name.is_none() && !(version >= 12) {
            return Err(EncodeError::NullField("Name"));
        }
        if is_flexible(version) {
            wire::put_compact_nullable_string(buf, self.name.as_deref());
        } else {
            wire::put_nullable_string(buf, self.name.as_deref())?;
        }
        if version >= 10 {
            wire::put_uuid(buf, self.topic_id);
        }
        if version >= 1 {
            buf.put_u8(u8::from(self.is_internal));
        }
        if is_flexible(version) {
            wire::put_compact_array_len(buf, Some(self.partitions.len()));
        } else {
            wire::put_array_len(buf, Some(self.partitions.len()))?;
        }
        for item in &self.partitions {
            item.encode(buf, version)?;
        }
        if version >= 8 {
            buf.put_i32(self.topic_authorized_operations);
        }
        if is_flexible(version) {
            wire::put_tagged_fields(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }

    pub fn decode(buf: &mut impl Buf, version: i16) -> Result<Self, DecodeError> {
        let mut this = Self::default();
        this.error_code = wire::get_i16(buf)?;
        this.name = {
            let raw = if is_flexible(version) {
                wire::get_compact_nullable_string(buf)?
            } else {
                wire::get_nullable_string(buf)?
            };
            if raw.is_none() && !(version >= 12) {
                return Err(DecodeError::InvalidLength(-1));
            }
            raw
        };
        if version >= 10 {
            this.topic_id = wire::get_uuid(buf)?;
        }
        if version >= 1 {
            this.is_internal = wire::get_bool(buf)?;
        }
        this.partitions = {
            let len = if is_flexible(version) {
                wire::get_compact_array_len(buf)?
            } else {
                wire::get_array_len(buf)?
            };
            match len {
                None => return Err(DecodeError::InvalidLength(-1)),
                Some(n) => {
                    let mut items = Vec::new();
                    for _ in 0..n {
                        items.push(MetadataResponsePartition::decode(buf, version)?);
                    }
                    items
                }
            }
        };
        if version >= 8 {
            this.topic_authorized_operations = wire::get_i32(buf)?;
        }
        if is_flexible(version) {
            this.unknown_tagged_fields = wire::get_tagged_fields(buf)?;
        }
        Ok(this)
    }
}

/// `MetadataResponsePartition` (nested in [`MetadataResponse`]).
#[derive(Debug, Clone, PartialEq)]
pub struct MetadataResponsePartition {
    /// The partition error, or 0 if there was no error.
    pub error_code: i16,
    /// The partition index.
    pub partition_index: i32,
    /// The ID of the leader broker.
    pub leader_id: i32,
    /// The leader epoch of this partition.
    pub leader_epoch: i32,
    /// The set of all nodes that host this partition.
    pub replica_nodes: Vec<i32>,
    /// The set of nodes that are in sync with the leader for this partition.
    pub isr_nodes: Vec<i32>,
    /// The set of offline replicas of this partition.
    pub offline_replicas: Vec<i32>,
    /// Tagged fields not (yet) known to this crate, kept raw.
    pub unknown_tagged_fields: Vec<RawTaggedField>,
}

impl Default for MetadataResponsePartition {
    fn default() -> Self {
        Self {
            error_code: 0,
            partition_index: 0,
            leader_id: 0,
            leader_epoch: -1,
            replica_nodes: Vec::new(),
            isr_nodes: Vec::new(),
            offline_replicas: Vec::new(),
            unknown_tagged_fields: Vec::new(),
        }
    }
}

impl MetadataResponsePartition {
    pub fn encode(&self, buf: &mut impl BufMut, version: i16) -> Result<(), EncodeError> {
        buf.put_i16(self.error_code);
        buf.put_i32(self.partition_index);
        buf.put_i32(self.leader_id);
        if version >= 7 {
            buf.put_i32(self.leader_epoch);
        }
        if is_flexible(version) {
            wire::put_compact_array_len(buf, Some(self.replica_nodes.len()));
        } else {
            wire::put_array_len(buf, Some(self.replica_nodes.len()))?;
        }
        for item in &self.replica_nodes {
            buf.put_i32(*item);
        }
        if is_flexible(version) {
            wire::put_compact_array_len(buf, Some(self.isr_nodes.len()));
        } else {
            wire::put_array_len(buf, Some(self.isr_nodes.len()))?;
        }
        for item in &self.isr_nodes {
            buf.put_i32(*item);
        }
        if version >= 5 {
            if is_flexible(version) {
                wire::put_compact_array_len(buf, Some(self.offline_replicas.len()));
            } else {
                wire::put_array_len(buf, Some(self.offline_replicas.len()))?;
            }
            for item in &self.offline_replicas {
                buf.put_i32(*item);
            }
        }
        if is_flexible(version) {
            wire::put_tagged_fields(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }

    pub fn decode(buf: &mut impl Buf, version: i16) -> Result<Self, DecodeError> {
        let mut this = Self::default();
        this.error_code = wire::get_i16(buf)?;
        this.partition_index = wire::get_i32(buf)?;
        this.leader_id = wire::get_i32(buf)?;
        if version >= 7 {
            this.leader_epoch = wire::get_i32(buf)?;
        }
        this.replica_nodes = {
            let len = if is_flexible(version) {
                wire::get_compact_array_len(buf)?
            } else {
                wire::get_array_len(buf)?
            };
            match len {
                None => return Err(DecodeError::InvalidLength(-1)),
                Some(n) => {
                    let mut items = Vec::new();
                    for _ in 0..n {
                        items.push(wire::get_i32(buf)?);
                    }
                    items
                }
            }
        };
        this.isr_nodes = {
            let len = if is_flexible(version) {
                wire::get_compact_array_len(buf)?
            } else {
                wire::get_array_len(buf)?
            };
            match len {
                None => return Err(DecodeError::InvalidLength(-1)),
                Some(n) => {
                    let mut items = Vec::new();
                    for _ in 0..n {
                        items.push(wire::get_i32(buf)?);
                    }
                    items
                }
            }
        };
        if version >= 5 {
            this.offline_replicas = {
                let len = if is_flexible(version) {
                    wire::get_compact_array_len(buf)?
                } else {
                    wire::get_array_len(buf)?
                };
                match len {
                    None => return Err(DecodeError::InvalidLength(-1)),
                    Some(n) => {
                        let mut items = Vec::new();
                        for _ in 0..n {
                            items.push(wire::get_i32(buf)?);
                        }
                        items
                    }
                }
            };
        }
        if is_flexible(version) {
            this.unknown_tagged_fields = wire::get_tagged_fields(buf)?;
        }
        Ok(this)
    }
}
